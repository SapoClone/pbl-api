diff --git a/build/cjs/src/v2/cloud_tasks_client.cjs b/build/cjs/src/v2/cloud_tasks_client.cjs
index 21a94e898f2185dec5c418e55e2ac394591cea27..83204ac18723fb2e9f69ea660df4383ffa5b3a73 100644
--- a/build/cjs/src/v2/cloud_tasks_client.cjs
+++ b/build/cjs/src/v2/cloud_tasks_client.cjs
@@ -373,8 +373,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name = request.name) !== null && _request$name !== void 0 ? _request$name : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('getQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -453,8 +454,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent = request.parent) !== null && _request$parent !== void 0 ? _request$parent : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('createQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -536,8 +538,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         'queue.name': (_name = request.queue.name) !== null && _name !== void 0 ? _name : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('updateQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -608,8 +611,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name2 = request.name) !== null && _request$name2 !== void 0 ? _request$name2 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('deleteQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -673,8 +677,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name3 = request.name) !== null && _request$name3 !== void 0 ? _request$name3 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('purgeQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -740,8 +745,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name4 = request.name) !== null && _request$name4 !== void 0 ? _request$name4 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('pauseQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -813,8 +819,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name5 = request.name) !== null && _request$name5 !== void 0 ? _request$name5 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('resumeQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -884,8 +891,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         resource: (_request$resource = request.resource) !== null && _request$resource !== void 0 ? _request$resource : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('getIamPolicy request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -965,8 +973,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         resource: (_request$resource2 = request.resource) !== null && _request$resource2 !== void 0 ? _request$resource2 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('setIamPolicy request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1037,8 +1046,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         resource: (_request$resource3 = request.resource) !== null && _request$resource3 !== void 0 ? _request$resource3 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('testIamPermissions request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1110,8 +1120,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name6 = request.name) !== null && _request$name6 !== void 0 ? _request$name6 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('getTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1223,8 +1234,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent2 = request.parent) !== null && _request$parent2 !== void 0 ? _request$parent2 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('createTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1287,8 +1299,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name7 = request.name) !== null && _request$name7 !== void 0 ? _request$name7 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('deleteTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1385,8 +1398,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name8 = request.name) !== null && _request$name8 !== void 0 ? _request$name8 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('runTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1481,8 +1495,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent3 = request.parent) !== null && _request$parent3 !== void 0 ? _request$parent3 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       var wrappedCallback = callback ? function (error, values, nextPageRequest, rawResponse) {
         _this14._log.info('listQueues values %j', values);
@@ -1560,8 +1575,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listQueues'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listQueues stream %j', request);
       return this.descriptors.page.listQueues.createStream(this.innerApiCalls.listQueues, request, callSettings);
@@ -1631,8 +1647,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listQueues'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listQueues iterate %j', request);
       return this.descriptors.page.listQueues.asyncIterate(this.innerApiCalls['listQueues'], request, callSettings);
@@ -1717,8 +1734,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent6 = request.parent) !== null && _request$parent6 !== void 0 ? _request$parent6 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       var wrappedCallback = callback ? function (error, values, nextPageRequest, rawResponse) {
         _this15._log.info('listTasks values %j', values);
@@ -1798,8 +1816,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listTasks'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listTasks stream %j', request);
       return this.descriptors.page.listTasks.createStream(this.innerApiCalls.listTasks, request, callSettings);
@@ -1871,8 +1890,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listTasks'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listTasks iterate %j', request);
       return this.descriptors.page.listTasks.asyncIterate(this.innerApiCalls['listTasks'], request, callSettings);
diff --git a/build/cjs/src/v2beta2/cloud_tasks_client.cjs b/build/cjs/src/v2beta2/cloud_tasks_client.cjs
index c55d8238b360373cfcc4e8676b36612bdf22c397..30b9c85ec4a880bea27645198d50c8b60b21e2da 100644
--- a/build/cjs/src/v2beta2/cloud_tasks_client.cjs
+++ b/build/cjs/src/v2beta2/cloud_tasks_client.cjs
@@ -378,8 +378,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name = request.name) !== null && _request$name !== void 0 ? _request$name : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('getQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -458,8 +459,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent = request.parent) !== null && _request$parent !== void 0 ? _request$parent : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('createQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -543,8 +545,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         'queue.name': (_name = request.queue.name) !== null && _name !== void 0 ? _name : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('updateQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -615,8 +618,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name2 = request.name) !== null && _request$name2 !== void 0 ? _request$name2 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('deleteQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -680,8 +684,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name3 = request.name) !== null && _request$name3 !== void 0 ? _request$name3 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('purgeQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -747,8 +752,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name4 = request.name) !== null && _request$name4 !== void 0 ? _request$name4 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('pauseQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -821,8 +827,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name5 = request.name) !== null && _request$name5 !== void 0 ? _request$name5 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('resumeQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -884,8 +891,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options = options || {};
       options.otherArgs = options.otherArgs || {};
       options.otherArgs.headers = options.otherArgs.headers || {};
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('uploadQueueYaml request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -955,8 +963,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         resource: (_request$resource = request.resource) !== null && _request$resource !== void 0 ? _request$resource : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('getIamPolicy request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1036,8 +1045,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         resource: (_request$resource2 = request.resource) !== null && _request$resource2 !== void 0 ? _request$resource2 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('setIamPolicy request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1108,8 +1118,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         resource: (_request$resource3 = request.resource) !== null && _request$resource3 !== void 0 ? _request$resource3 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('testIamPermissions request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1182,8 +1193,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name6 = request.name) !== null && _request$name6 !== void 0 ? _request$name6 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('getTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1300,8 +1312,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent2 = request.parent) !== null && _request$parent2 !== void 0 ? _request$parent2 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('createTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1364,8 +1377,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name7 = request.name) !== null && _request$name7 !== void 0 ? _request$name7 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('deleteTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1527,8 +1541,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent3 = request.parent) !== null && _request$parent3 !== void 0 ? _request$parent3 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('leaseTasks request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1606,8 +1621,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name8 = request.name) !== null && _request$name8 !== void 0 ? _request$name8 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('acknowledgeTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1697,8 +1713,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name9 = request.name) !== null && _request$name9 !== void 0 ? _request$name9 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('renewLease request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1782,8 +1799,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name0 = request.name) !== null && _request$name0 !== void 0 ? _request$name0 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('cancelLease request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1885,8 +1903,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name1 = request.name) !== null && _request$name1 !== void 0 ? _request$name1 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('runTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1986,8 +2005,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent4 = request.parent) !== null && _request$parent4 !== void 0 ? _request$parent4 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       var wrappedCallback = callback ? function (error, values, nextPageRequest, rawResponse) {
         _this19._log.info('listQueues values %j', values);
@@ -2070,8 +2090,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listQueues'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listQueues stream %j', request);
       return this.descriptors.page.listQueues.createStream(this.innerApiCalls.listQueues, request, callSettings);
@@ -2146,8 +2167,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listQueues'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listQueues iterate %j', request);
       return this.descriptors.page.listQueues.asyncIterate(this.innerApiCalls['listQueues'], request, callSettings);
@@ -2233,8 +2255,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent7 = request.parent) !== null && _request$parent7 !== void 0 ? _request$parent7 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       var wrappedCallback = callback ? function (error, values, nextPageRequest, rawResponse) {
         _this20._log.info('listTasks values %j', values);
@@ -2315,8 +2338,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listTasks'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listTasks stream %j', request);
       return this.descriptors.page.listTasks.createStream(this.innerApiCalls.listTasks, request, callSettings);
@@ -2389,8 +2413,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listTasks'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listTasks iterate %j', request);
       return this.descriptors.page.listTasks.asyncIterate(this.innerApiCalls['listTasks'], request, callSettings);
diff --git a/build/cjs/src/v2beta3/cloud_tasks_client.cjs b/build/cjs/src/v2beta3/cloud_tasks_client.cjs
index dc5ca960782838e0f135277ac2294e67b942b106..d5fd67ade6a00442a7c9c0dc876941a76516b8e7 100644
--- a/build/cjs/src/v2beta3/cloud_tasks_client.cjs
+++ b/build/cjs/src/v2beta3/cloud_tasks_client.cjs
@@ -414,8 +414,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name = request.name) !== null && _request$name !== void 0 ? _request$name : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('getQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -494,8 +495,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent = request.parent) !== null && _request$parent !== void 0 ? _request$parent : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('createQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -579,8 +581,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         'queue.name': (_name = request.queue.name) !== null && _name !== void 0 ? _name : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('updateQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -658,8 +661,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name2 = request.name) !== null && _request$name2 !== void 0 ? _request$name2 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('deleteQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -723,8 +727,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name3 = request.name) !== null && _request$name3 !== void 0 ? _request$name3 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('purgeQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -790,8 +795,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name4 = request.name) !== null && _request$name4 !== void 0 ? _request$name4 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('pauseQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -864,8 +870,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name5 = request.name) !== null && _request$name5 !== void 0 ? _request$name5 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('resumeQueue request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -935,8 +942,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         resource: (_request$resource = request.resource) !== null && _request$resource !== void 0 ? _request$resource : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('getIamPolicy request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1016,8 +1024,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         resource: (_request$resource2 = request.resource) !== null && _request$resource2 !== void 0 ? _request$resource2 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('setIamPolicy request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1088,8 +1097,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         resource: (_request$resource3 = request.resource) !== null && _request$resource3 !== void 0 ? _request$resource3 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('testIamPermissions request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1166,8 +1176,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name6 = request.name) !== null && _request$name6 !== void 0 ? _request$name6 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('getTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1279,8 +1290,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent2 = request.parent) !== null && _request$parent2 !== void 0 ? _request$parent2 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('createTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1343,8 +1355,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name7 = request.name) !== null && _request$name7 !== void 0 ? _request$name7 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('deleteTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1443,8 +1456,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name8 = request.name) !== null && _request$name8 !== void 0 ? _request$name8 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('runTask request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1509,8 +1523,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         'cmek_config.name': (_name2 = request.cmekConfig.name) !== null && _name2 !== void 0 ? _name2 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('updateCmekConfig request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1572,8 +1587,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         name: (_request$name9 = request.name) !== null && _request$name9 !== void 0 ? _request$name9 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('getCmekConfig request %j', request);
       var wrappedCallback = callback ? function (error, response, options, rawResponse) {
@@ -1653,8 +1669,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent3 = request.parent) !== null && _request$parent3 !== void 0 ? _request$parent3 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       var wrappedCallback = callback ? function (error, response, rawResponse, _) {
         _this16._log.info('batchCreateTasks response %j', rawResponse);
@@ -1763,8 +1780,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent4 = request.parent) !== null && _request$parent4 !== void 0 ? _request$parent4 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       var wrappedCallback = callback ? function (error, response, rawResponse, _) {
         _this17._log.info('batchDeleteTasks response %j', rawResponse);
@@ -1897,8 +1915,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent5 = request.parent) !== null && _request$parent5 !== void 0 ? _request$parent5 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       var wrappedCallback = callback ? function (error, values, nextPageRequest, rawResponse) {
         _this18._log.info('listQueues values %j', values);
@@ -1981,8 +2000,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listQueues'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listQueues stream %j', request);
       return this.descriptors.page.listQueues.createStream(this.innerApiCalls.listQueues, request, callSettings);
@@ -2057,8 +2077,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listQueues'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listQueues iterate %j', request);
       return this.descriptors.page.listQueues.asyncIterate(this.innerApiCalls['listQueues'], request, callSettings);
@@ -2144,8 +2165,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       options.otherArgs.headers['x-goog-request-params'] = this._gaxModule.routingHeader.fromParams({
         parent: (_request$parent8 = request.parent) !== null && _request$parent8 !== void 0 ? _request$parent8 : ''
       });
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       var wrappedCallback = callback ? function (error, values, nextPageRequest, rawResponse) {
         _this19._log.info('listTasks values %j', values);
@@ -2226,8 +2248,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listTasks'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listTasks stream %j', request);
       return this.descriptors.page.listTasks.createStream(this.innerApiCalls.listTasks, request, callSettings);
@@ -2300,8 +2323,9 @@ var CloudTasksClient = exports.CloudTasksClient = /*#__PURE__*/function () {
       });
       var defaultCallSettings = this._defaults['listTasks'];
       var callSettings = defaultCallSettings.merge(options);
-      this.initialize()["catch"](function (err) {
-        throw err;
+      this.initialize()["catch"](function () {
+        // Errors are already surfaced via the main call's returned promise;
+        // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
       });
       this._log.info('listTasks iterate %j', request);
       return this.descriptors.page.listTasks.asyncIterate(this.innerApiCalls['listTasks'], request, callSettings);
diff --git a/build/esm/src/v2/cloud_tasks_client.js b/build/esm/src/v2/cloud_tasks_client.js
index e9f80a0413d26e47a60855bca5ee8d3814b2b5b7..e5aefec6cf667bca2d05df8d3a9c80bb9bd86af0 100644
--- a/build/esm/src/v2/cloud_tasks_client.js
+++ b/build/esm/src/v2/cloud_tasks_client.js
@@ -334,8 +334,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('getQueue request %j', request);
         const wrappedCallback = callback
@@ -377,8 +378,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('createQueue request %j', request);
         const wrappedCallback = callback
@@ -420,8 +422,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 'queue.name': request.queue.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('updateQueue request %j', request);
         const wrappedCallback = callback
@@ -463,8 +466,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('deleteQueue request %j', request);
         const wrappedCallback = callback
@@ -506,8 +510,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('purgeQueue request %j', request);
         const wrappedCallback = callback
@@ -549,8 +554,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('pauseQueue request %j', request);
         const wrappedCallback = callback
@@ -592,8 +598,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('resumeQueue request %j', request);
         const wrappedCallback = callback
@@ -635,8 +642,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 resource: request.resource ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('getIamPolicy request %j', request);
         const wrappedCallback = callback
@@ -678,8 +686,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 resource: request.resource ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('setIamPolicy request %j', request);
         const wrappedCallback = callback
@@ -721,8 +730,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 resource: request.resource ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('testIamPermissions request %j', request);
         const wrappedCallback = callback
@@ -764,8 +774,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('getTask request %j', request);
         const wrappedCallback = callback
@@ -807,8 +818,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('createTask request %j', request);
         const wrappedCallback = callback
@@ -850,8 +862,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('deleteTask request %j', request);
         const wrappedCallback = callback
@@ -893,8 +906,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('runTask request %j', request);
         const wrappedCallback = callback
@@ -936,8 +950,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         const wrappedCallback = callback
             ? (error, values, nextPageRequest, rawResponse) => {
@@ -1012,8 +1027,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listQueues'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listQueues stream %j', request);
         return this.descriptors.page.listQueues.createStream(this.innerApiCalls.listQueues, request, callSettings);
@@ -1080,8 +1096,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listQueues'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listQueues iterate %j', request);
         return this.descriptors.page.listQueues.asyncIterate(this.innerApiCalls['listQueues'], request, callSettings);
@@ -1103,8 +1120,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         const wrappedCallback = callback
             ? (error, values, nextPageRequest, rawResponse) => {
@@ -1181,8 +1199,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listTasks'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listTasks stream %j', request);
         return this.descriptors.page.listTasks.createStream(this.innerApiCalls.listTasks, request, callSettings);
@@ -1251,8 +1270,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listTasks'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listTasks iterate %j', request);
         return this.descriptors.page.listTasks.asyncIterate(this.innerApiCalls['listTasks'], request, callSettings);
diff --git a/build/esm/src/v2beta2/cloud_tasks_client.js b/build/esm/src/v2beta2/cloud_tasks_client.js
index 25a2b63dcbb88bd37acf7f970781674f51a6dc7d..1bde3e87bdc60352f67022af79d119c1320b15c8 100644
--- a/build/esm/src/v2beta2/cloud_tasks_client.js
+++ b/build/esm/src/v2beta2/cloud_tasks_client.js
@@ -339,8 +339,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('getQueue request %j', request);
         const wrappedCallback = callback
@@ -382,8 +383,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('createQueue request %j', request);
         const wrappedCallback = callback
@@ -425,8 +427,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 'queue.name': request.queue.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('updateQueue request %j', request);
         const wrappedCallback = callback
@@ -468,8 +471,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('deleteQueue request %j', request);
         const wrappedCallback = callback
@@ -511,8 +515,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('purgeQueue request %j', request);
         const wrappedCallback = callback
@@ -554,8 +559,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('pauseQueue request %j', request);
         const wrappedCallback = callback
@@ -597,8 +603,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('resumeQueue request %j', request);
         const wrappedCallback = callback
@@ -636,8 +643,9 @@ export class CloudTasksClient {
         options = options || {};
         options.otherArgs = options.otherArgs || {};
         options.otherArgs.headers = options.otherArgs.headers || {};
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('uploadQueueYaml request %j', request);
         const wrappedCallback = callback
@@ -679,8 +687,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 resource: request.resource ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('getIamPolicy request %j', request);
         const wrappedCallback = callback
@@ -722,8 +731,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 resource: request.resource ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('setIamPolicy request %j', request);
         const wrappedCallback = callback
@@ -765,8 +775,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 resource: request.resource ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('testIamPermissions request %j', request);
         const wrappedCallback = callback
@@ -808,8 +819,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('getTask request %j', request);
         const wrappedCallback = callback
@@ -851,8 +863,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('createTask request %j', request);
         const wrappedCallback = callback
@@ -894,8 +907,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('deleteTask request %j', request);
         const wrappedCallback = callback
@@ -937,8 +951,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('leaseTasks request %j', request);
         const wrappedCallback = callback
@@ -980,8 +995,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('acknowledgeTask request %j', request);
         const wrappedCallback = callback
@@ -1023,8 +1039,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('renewLease request %j', request);
         const wrappedCallback = callback
@@ -1066,8 +1083,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('cancelLease request %j', request);
         const wrappedCallback = callback
@@ -1109,8 +1127,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('runTask request %j', request);
         const wrappedCallback = callback
@@ -1152,8 +1171,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         const wrappedCallback = callback
             ? (error, values, nextPageRequest, rawResponse) => {
@@ -1233,8 +1253,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listQueues'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listQueues stream %j', request);
         return this.descriptors.page.listQueues.createStream(this.innerApiCalls.listQueues, request, callSettings);
@@ -1306,8 +1327,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listQueues'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listQueues iterate %j', request);
         return this.descriptors.page.listQueues.asyncIterate(this.innerApiCalls['listQueues'], request, callSettings);
@@ -1329,8 +1351,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         const wrappedCallback = callback
             ? (error, values, nextPageRequest, rawResponse) => {
@@ -1408,8 +1431,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listTasks'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listTasks stream %j', request);
         return this.descriptors.page.listTasks.createStream(this.innerApiCalls.listTasks, request, callSettings);
@@ -1479,8 +1503,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listTasks'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listTasks iterate %j', request);
         return this.descriptors.page.listTasks.asyncIterate(this.innerApiCalls['listTasks'], request, callSettings);
diff --git a/build/esm/src/v2beta3/cloud_tasks_client.js b/build/esm/src/v2beta3/cloud_tasks_client.js
index 688a2e76ee5cf51774fcde2a05540de719d97cb8..f3ebdd72770c02032e2c8448d82f9c96945f3652 100644
--- a/build/esm/src/v2beta3/cloud_tasks_client.js
+++ b/build/esm/src/v2beta3/cloud_tasks_client.js
@@ -378,8 +378,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('getQueue request %j', request);
         const wrappedCallback = callback
@@ -421,8 +422,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('createQueue request %j', request);
         const wrappedCallback = callback
@@ -464,8 +466,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 'queue.name': request.queue.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('updateQueue request %j', request);
         const wrappedCallback = callback
@@ -507,8 +510,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('deleteQueue request %j', request);
         const wrappedCallback = callback
@@ -550,8 +554,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('purgeQueue request %j', request);
         const wrappedCallback = callback
@@ -593,8 +598,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('pauseQueue request %j', request);
         const wrappedCallback = callback
@@ -636,8 +642,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('resumeQueue request %j', request);
         const wrappedCallback = callback
@@ -679,8 +686,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 resource: request.resource ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('getIamPolicy request %j', request);
         const wrappedCallback = callback
@@ -722,8 +730,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 resource: request.resource ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('setIamPolicy request %j', request);
         const wrappedCallback = callback
@@ -765,8 +774,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 resource: request.resource ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('testIamPermissions request %j', request);
         const wrappedCallback = callback
@@ -808,8 +818,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('getTask request %j', request);
         const wrappedCallback = callback
@@ -851,8 +862,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('createTask request %j', request);
         const wrappedCallback = callback
@@ -894,8 +906,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('deleteTask request %j', request);
         const wrappedCallback = callback
@@ -937,8 +950,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('runTask request %j', request);
         const wrappedCallback = callback
@@ -980,8 +994,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 'cmek_config.name': request.cmekConfig.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('updateCmekConfig request %j', request);
         const wrappedCallback = callback
@@ -1023,8 +1038,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 name: request.name ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('getCmekConfig request %j', request);
         const wrappedCallback = callback
@@ -1066,8 +1082,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         const wrappedCallback = callback
             ? (error, response, rawResponse, _) => {
@@ -1118,8 +1135,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         const wrappedCallback = callback
             ? (error, response, rawResponse, _) => {
@@ -1170,8 +1188,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         const wrappedCallback = callback
             ? (error, values, nextPageRequest, rawResponse) => {
@@ -1251,8 +1270,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listQueues'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listQueues stream %j', request);
         return this.descriptors.page.listQueues.createStream(this.innerApiCalls.listQueues, request, callSettings);
@@ -1324,8 +1344,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listQueues'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listQueues iterate %j', request);
         return this.descriptors.page.listQueues.asyncIterate(this.innerApiCalls['listQueues'], request, callSettings);
@@ -1347,8 +1368,9 @@ export class CloudTasksClient {
             this._gaxModule.routingHeader.fromParams({
                 parent: request.parent ?? '',
             });
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         const wrappedCallback = callback
             ? (error, values, nextPageRequest, rawResponse) => {
@@ -1426,8 +1448,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listTasks'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listTasks stream %j', request);
         return this.descriptors.page.listTasks.createStream(this.innerApiCalls.listTasks, request, callSettings);
@@ -1497,8 +1520,9 @@ export class CloudTasksClient {
             });
         const defaultCallSettings = this._defaults['listTasks'];
         const callSettings = defaultCallSettings.merge(options);
-        this.initialize().catch(err => {
-            throw err;
+        this.initialize().catch(() => {
+            // Errors are already surfaced via the main call's returned promise;
+            // swallow here to avoid an orphaned unhandled rejection (PBL6 patch).
         });
         this._log.info('listTasks iterate %j', request);
         return this.descriptors.page.listTasks.asyncIterate(this.innerApiCalls['listTasks'], request, callSettings);
